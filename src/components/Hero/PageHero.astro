---
import LeftRightWithTransparentImage from "./left_right_with_transparent_image/LeftRightWithTransparentImage.astro";

const { title, excerpt, image } = Astro.props;
---

<LeftRightWithTransparentImage title={title} excerpt={excerpt} image={image} />
